# dependencies
node_modules

# build output
.next
out
*.tsbuildinfo

# environment secrets — never commit real credentials
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem
npm-debug.log*
.vercel

# raw logo assets before background removal
assets/logo-source.*

# runtime logs / tool state
.preview-*.log
.freebuff/
